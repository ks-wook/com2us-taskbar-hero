# GameServer 구현 오류 리뷰

- 검토 일시: 2026-08-26 18:13 KST
- 검토 범위: `GameServer/`의 컨트롤러, 서비스, 리포지토리, 배치, Redis 연동 코드
- 보조 검토 범위: `TaskbarHero.Common/` DTO와 `docs/공통/db-schema.sql`
- 검토 방식: 정적 코드 분석, 데이터베이스 상태 전이 및 동시성 검토, 프로젝트 빌드 검증

## 1. 요약

`GameServer` 프로젝트는 컴파일 경고와 오류 없이 빌드된다. 그러나 운영 데이터 정합성을 훼손할 수 있는 구현 문제가 존재한다. 가장 우선해서 수정해야 할 문제는 보스러시 시즌 정산의 복구 불가능 상태와 동일 사용자의 동시 요청에서 발생하는 재화 과소 차감 및 보상 유실이다.

| 심각도 | 발견 사항 | 주요 영향 | 상태 |
|---|---|---|---|
| 치명 | 보스러시 정산이 중간에 끊기면 복구되지 않음 | 시즌 정산과 다음 시즌 개시가 모두 중단됨 | **해결** (6.1) |
| 치명 | 개별 순위 정산 실패 후에도 시즌을 닫음 | 일부 사용자의 순위와 보상이 영구 누락됨 | **해결** (6.2) |
| 높음 | 사용자 경제 트랜잭션에 직렬화가 없음 | 재화 과소 차감, 보상 유실, 상태 덮어쓰기가 발생함 | **해결** (6.3) |
| 중간 | 보스러시 동률 순서가 결정적이지 않음 | Redis 순위와 최종 DB 순위가 달라질 수 있음 | **해결** (6.3) |
| 중간 | `ServerError`가 HTTP 400으로 변환됨 | 서버 장애가 클라이언트 오류로 잘못 분류됨 | **해결** (6.4) |
| 중간 | 요청 배열의 `null` 요소를 검사하지 않음 | 잘못된 요청이 HTTP 500을 발생시킴 | **해결** (6.6) |
| 중간 | 닉네임 길이 검증이 없음 | DB 저장 오류가 발생함 | **해결** (6.7) |

## 2. 상세 발견 사항

### 2.1 치명 — 보스러시 정산이 `Settling` 상태에서 영구 중단될 수 있음

시즌 정산은 다음 순서로 실행된다.

1. 현재 `Running` 시즌을 조회한다.
2. 시즌 상태를 `Running`에서 `Settling`으로 변경한다.
3. 참가자의 순위와 보상을 정산한다.
4. 시즌 상태를 `Closed`로 변경한다.
5. 다음 시즌을 생성한다.

각 단계가 하나의 원자적 상태 전이가 아니라 별도 DB 호출과 트랜잭션으로 실행된다. 시즌을 `Settling`으로 변경한 뒤 프로세스가 종료되거나 정산 중 처리되지 않은 예외가 발생하면, 다음 실행에서는 `Running` 시즌만 조회하므로 해당 시즌을 찾지 못한다.

진행 중 시즌을 찾지 못한 스케줄러는 `_nextWakeUnix`를 0으로 설정하고 `Timeout.InfiniteTimeSpan`을 반환한다. 코드에는 `Wake()` 메서드가 있지만 실제 호출자가 없으므로 서버가 살아 있어도 스케줄러가 다시 실행되지 않는다. 시즌을 `Closed`로 변경한 뒤 다음 시즌 생성 전에 장애가 발생하는 경우에도 같은 중단 상태가 만들어진다.

근거 코드는 다음과 같다.

- [`BossRushSeasonBatchScheduler.RunCycleAsync`](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L104)
- [`BossRushSeasonBatchScheduler.NextDelay`](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L81)
- [`BossRushRepository.GetRunningSeasonAsync`](../GameServer/Repositories/GameDb/BossRushRepository.cs#L101)
- [`BossRushRepository.ClaimSeasonForSettlementAsync`](../GameServer/Repositories/GameDb/BossRushRepository.cs#L476)
- [`BossRushRepository.CloseSeasonAsync`](../GameServer/Repositories/GameDb/BossRushRepository.cs#L558)

권장 수정 방향은 다음과 같다.

- 서버 시작 시 `Settling` 시즌을 우선 조회하고 정산을 재개해야 한다.
- `Closed` 시즌은 존재하지만 다음 시즌이 없는 상태를 탐지해 다음 시즌을 생성해야 한다.
- 정산 단계별 상태를 명시적으로 기록하고 모든 단계가 멱등적으로 재실행되게 해야 한다.
- 외부 기상 신호가 확실하게 연결되기 전에는 시즌 부재 시 무기한 대기하지 않아야 한다.

### 2.2 치명 — 개별 순위 정산 실패 후에도 시즌을 닫음

`SettleAsync`는 참가자 한 명의 정산 중 예외가 발생하면 예외를 잡아 실패 건수만 증가시키고 다음 참가자를 계속 처리한다. 실패한 참가자의 순위가 저장되지 않았는데도 `rank` 값은 항상 증가한다.

반복이 끝난 뒤 실패 건수가 존재하더라도 호출자는 시즌을 무조건 `Closed`로 변경하고 다음 시즌을 시작한다. 따라서 실패한 참가자는 `final_rank=0` 상태로 영구 잔류하고 순위 보상도 받지 못한다. 이후 참가자의 순위에는 빈 번호가 생긴다.

`boss_rush_record`의 `(season_id, final_rank)` 인덱스는 유니크 인덱스가 아니므로 잘못된 최종 순위를 데이터베이스가 차단하지도 않는다.

근거 코드는 다음과 같다.

- [`BossRushSeasonBatchScheduler.SettleAsync`](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L165)
- [개별 정산 예외 처리](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L221)
- [성공 여부와 무관한 `rank++`](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L227)
- [실패 건수와 무관한 시즌 종료](../GameServer/Batch/BossRushSeasonBatchScheduler.cs#L145)
- [`boss_rush_record` 인덱스](공통/db-schema.sql#L440)

권장 수정 방향은 다음과 같다.

- 정산 한 건이라도 실패하면 시즌을 닫지 않아야 한다.
- 순위는 정산 저장에 성공한 경우에만 증가시켜야 한다.
- 실패 지점부터 동일한 정렬 순서로 재개할 수 있어야 한다.
- 순위 저장과 보상 메일 발급을 하나의 멱등 트랜잭션으로 유지해야 한다.

### 2.3 높음 — 동일 사용자의 경제 트랜잭션이 직렬화되지 않음

여러 상태 변경 트랜잭션이 사용자 또는 재화 행을 잠그지 않은 상태에서 다음 패턴을 사용한다.

```text
현재 값 SELECT → 애플리케이션에서 새 값 계산 → 계산된 절대값 UPDATE
```

트랜잭션 자체는 여러 테이블 변경을 함께 커밋하거나 롤백하는 원자성을 제공하지만, 잠금 전에 읽은 이전 값을 이용한 덮어쓰기까지 방지하지는 않는다.

예를 들어 골드 100을 보유한 사용자가 가격 60인 서로 다른 거래 물품 두 개를 동시에 구매하면 두 트랜잭션이 모두 잔액 100을 읽을 수 있다. 각 트랜잭션은 서로 다른 거래 등록 행을 정상적으로 선점하고, 마지막에 모두 잔액 40을 기록할 수 있다. 사용자는 물품 두 개를 받지만 골드는 한 번 분량만 차감되는 결과가 된다.

동일한 패턴이 다음 경로에 존재한다.

- [거래 구매의 골드 조회와 절대값 갱신](../GameServer/Repositories/GameDb/TradeRepository.cs#L259)
- [가챠 비용 차감](../GameServer/Repositories/GameDb/GachaRepository.cs#L104)
- [캐릭터 추가 비용 차감](../GameServer/Repositories/GameDb/SaveRepository.cs#L313)
- [메일 골드 지급](../GameServer/Repositories/GameDb/MailRepository.cs#L395)
- [스테이지 골드 보상](../GameServer/Repositories/GameDb/StageRepository.cs#L260)
- [스테이지 리포지토리의 동시 요청 경고 주석](../GameServer/Repositories/GameDb/StageRepository.cs#L111)

이 문제는 재화뿐 아니라 캐릭터 경험치, 가챠 천장 횟수, 인벤토리 스택과 슬롯 할당에도 영향을 줄 수 있다.

권장 수정 방향은 다음과 같다.

- 모든 사용자 상태 변경 트랜잭션 시작 시 `game_player`의 해당 사용자 행을 `FOR UPDATE`로 잠가 사용자 단위로 직렬화해야 한다.
- 재화 차감은 `quantity >= cost` 조건을 포함하는 원자적 `UPDATE`와 영향 행 수 검사로 구현하는 방안을 병행해야 한다.
- 인벤토리 슬롯과 스택 할당에도 동일한 잠금 순서를 적용해 교착 상태를 예방해야 한다.
- 서로 다른 DB 연결 두 개로 동일 사용자 요청을 동시에 실행하는 통합 테스트를 추가해야 한다.

### 2.4 중간 — 보스러시 동률 순서가 결정적이지 않음

MySQL 순위 조회는 `best_clear_ms`, `recorded_at`까지만 정렬한다. 두 값이 모두 같은 사용자가 존재하면 최종 순서는 보장되지 않는다.

Redis 점수는 클리어 시간과 시즌 시작 이후의 초 단위 시각을 결합한다. 두 사용자의 기록이 같은 밀리초이고 같은 초에 저장되면 Redis 점수도 동일하다. Redis는 동일 점수에서 멤버 문자열을 기준으로 순서를 결정하지만 MySQL은 같은 기준을 사용하지 않는다.

따라서 실시간 Redis 순위, MySQL 폴백 순위, 시즌 최종 정산 순위가 서로 달라질 수 있다.

근거 코드는 다음과 같다.

- [MySQL 순위 정렬](../GameServer/Repositories/GameDb/BossRushRepository.cs#L465)
- [정산 대상 순위 정렬](../GameServer/Repositories/GameDb/BossRushRepository.cs#L509)
- [Redis 점수 생성](../GameServer/Repositories/MemoryDb/BossRushRankCache.cs#L193)

MySQL, Redis, 정산 배치가 모두 공유하는 최종 동률 기준을 정의해야 한다. `user_id` 같은 안정적인 값을 최종 기준으로 사용할 수 있지만 Redis의 `double` 점수에 큰 정수를 무리하게 압축하지 않도록 별도의 정렬 전략이 필요하다.

### 2.5 중간 — `ErrorCode.ServerError`가 HTTP 400으로 변환됨

`GameApiControllerBase.HttpStatus`는 명시적으로 분류되지 않은 모든 에러 코드를 HTTP 400으로 처리한다. `ErrorCode.ServerError`의 500 매핑은 존재하지 않는다.

반면 `ConsumableService`와 `MailService`는 예상하지 못한 리포지토리 상태를 `ErrorCode.ServerError`로 변환한다. 이 경로에서는 실제 서버 결함이 HTTP 400으로 반환되어 클라이언트 오류로 잘못 분류된다. 모니터링과 재시도 정책에도 잘못된 신호를 제공한다.

근거 코드는 다음과 같다.

- [`GameApiControllerBase.HttpStatus`](../GameServer/Controllers/GameApiControllerBase.cs#L34)
- [`ConsumableService`의 기본 오류 변환](../GameServer/Services/ConsumableService.cs#L159)
- [`MailService`의 기본 오류 변환](../GameServer/Services/MailService.cs#L207)

`ErrorCode.ServerError`를 HTTP 500으로 명시적으로 매핑해야 한다.

### 2.6 중간 — 요청 배열의 `null` 요소가 HTTP 500을 발생시킴

공통 DTO의 배열 요소는 참조형 클래스다. JSON 요청 배열에는 `null` 요소가 들어올 수 있지만 서비스는 각 요소의 null 여부를 확인하지 않고 필드에 바로 접근한다.

다음 요청 형태가 대표적인 예다.

```json
{
  "members": [null]
}
```

이 요청은 유효성 오류로 400을 반환해야 하지만 현재 코드에서는 `NullReferenceException`이 발생해 전역 예외 처리기의 500 응답으로 변환될 수 있다.

영향 경로는 다음과 같다.

- [파티 편성 검증](../GameServer/Services/SaveService.cs#L296)
- [큐브 분해 요청 변환](../GameServer/Services/CubeService.cs#L111)
- [보스러시 라운드 진행 검증](../GameServer/Services/BossRushService.cs#L440)

서비스 또는 공통 요청 검증 계층에서 배열의 모든 요소가 null이 아님을 확인하고 잘못된 요청 에러로 반환해야 한다.

### 2.7 중간 — 닉네임 길이 검증이 없음

캐릭터 생성은 닉네임이 비어 있는지만 확인하고 `Trim()`한 값을 DB에 전달한다. `game_player.nickname` 컬럼은 `VARCHAR(50)`이다.

50자를 초과하는 닉네임이 전달되면 MySQL SQL 모드에 따라 데이터 길이 오류로 HTTP 500이 발생하거나 문자열이 비의도적으로 잘릴 수 있다.

근거 코드는 다음과 같다.

- [닉네임 공백 검증](../GameServer/Services/SaveService.cs#L159)
- [`game_player.nickname` 스키마](공통/db-schema.sql#L88)

DB 트랜잭션을 시작하기 전에 닉네임의 최소·최대 길이와 허용 문자 정책을 검증해야 한다.

## 3. 검증 결과와 한계

다음 명령으로 프로젝트를 검증했다.

```powershell
dotnet build GameServer/GameServer.csproj --no-restore
```

검증 결과는 다음과 같다.

- 빌드는 경고 0개, 오류 0개로 성공했다.
- 저장소에 자동 테스트 프로젝트가 존재하지 않았다.
- 서버와 MySQL·Redis를 기동한 통합 테스트는 수행하지 않았다.
- 동시성 문제는 코드의 잠금 부재와 InnoDB의 일반적인 읽기·갱신 동작을 기준으로 분석했다.
- GitNexus 지식 그래프는 LadybugDB shadow page 복구 오류로 열리지 않아, GameServer 전체 소스와 관련 DTO·DB 스키마를 직접 대조했다.

## 4. 권장 수정 우선순위

### P0 — 즉시 수정

1. 보스러시 `Settling` 시즌 복구 경로를 구현해야 한다.
2. 정산 실패가 존재하면 시즌을 닫지 않도록 변경해야 한다.
3. 사용자 상태 변경 트랜잭션이 서로 덮어쓰지 않게 해야 한다.

### P1 — P0 이후 수정

1. 보스러시 동률 기준을 MySQL과 Redis에 동일하게 적용해야 한다.
2. `ServerError`를 HTTP 500으로 매핑해야 한다.
3. 요청 배열 요소와 닉네임 길이 검증을 추가해야 한다.

## 5. 권장 회귀 테스트

- 시즌을 `Settling`으로 변경한 직후 프로세스를 종료하고 재기동해 정산이 재개되는지 검증해야 한다.
- 참가자 한 명의 정산에서 강제로 예외를 발생시키고 시즌이 닫히지 않는지 검증해야 한다.
- 시즌 종료 직후 다음 시즌 생성 전에 장애가 발생한 상태를 복구하는지 검증해야 한다.
- 두 DB 연결에서 동일 사용자가 서로 다른 거래 물품을 동시에 구매하는 테스트를 추가해야 한다.
- 동일 사용자의 가챠, 메일 수령, 스테이지 클리어 요청을 동시에 실행해 재화와 아이템 총량을 검증해야 한다.
- 요청 배열에 `null` 요소를 넣었을 때 HTTP 400이 반환되는지 검증해야 한다.
- 50자를 초과하는 닉네임이 DB 접근 전에 거부되는지 검증해야 한다.

---

## 6. 조치 내역

2장의 발견 사항 순서대로 정리합니다. 각 항목은 **무엇이 문제였나 → 논의에서 정한 것 → 어떻게 고쳤나**
순으로, 방향만 짧게 적습니다.

| 항목 | 내용 | 상태 | 조치 결과 |
|---|---|---|---|
| [6.1](#61-21--보스러시-정산이-중간에-끊기면-복구되지-않음) | 보스러시 정산이 중간에 끊기면 복구되지 않음 | 조치 완료 | '정산 중' 상태를 미완료 상태로 다시 인지하여, 깨어난 배치가 정산이 완료되지 않은 시즌부터 이어서 정산한다 |
| [6.2](#62-22--개별-순위-정산이-실패해도-시즌을-닫음) | 개별 순위 정산이 실패해도 시즌을 닫음 | 조치 완료 | 정산이 끝까지 돌았을 때만 시즌을 닫는다. |
| [6.3](#63-23--같은-사용자의-값-변경이-겹치면-덮어써짐) | 같은 사용자의 값 변경이 겹치면 덮어써짐 | 조치 완료 | 트랜잭션 시작 시 조회한 값을 그대로 쓰지 않는다. 재화는 Update 쿼리 안에서 현재값을 기준으로 계산하고, 계산이 필요한 나머지 값(경험치·진행도·단계·수량·가방 칸)은 **잠금 조회로 앞 요청을 기다렸다가 최신값을 읽어** 처리한다. 겹친 요청은 거절되지 않고 순서대로 모두 반영된다. |
| [6.4](#64-24--보스러시-동점자-순서가-정해져-있지-않음) | 보스러시 동점자 순서가 정해져 있지 않음 | 조치 완료 | 동점이면 `user_id`가 작은 쪽을 위로 두도록 MySQL·Redis에 같은 기준을 적용했다 |
| [6.5](#65-25--servererror가-http-400으로-나감) | `ServerError`가 HTTP 400으로 나감 | 조치 완료 | 베이스 컨트롤러에서 `ServerError`를 HTTP 500으로 매핑했다 |
| [6.6](#66-26--요청-배열의-null-요소가-http-500을-만듦) | 요청 배열의 `null` 요소가 HTTP 500을 만듦 | 조치 완료 | 목록을 받는 세 요청의 형식 검증에 요소 `null` 검사를 넣어 `InvalidRequest(1006)`·400으로 거절한다 |
| [6.7](#67-27--닉네임-길이-검증이-없음) | 닉네임 길이 검증이 없음 | 조치 완료 | 닉네임을 받는 두 경로(회원가입·최초 캐릭터 생성)에서 DB에 닿기 전에 최대 12자를 검사하고 `NicknameTooLong(1007)`으로 거절한다 |

### 6.1 (2.1) — 보스러시 정산이 중간에 끊기면 복구되지 않음

**문제.** 보스러시 시즌 정산은 ① 시즌을 '정산 중'으로 바꾸고 → ② 한 명씩 순위를 매겨 보상 메일을 보내고 → ③ 시즌을 닫고
다음 시즌을 여는 순서로 돕니다. **②에서 배치가 멈추면(재시작·배포·DB 순단) 그 시즌은 '정산 중'인 채로
영원히 남았습니다.** 다음번에 깨어난 배치가 '진행 중'인 시즌만 찾았기 때문입니다. 순위를 못 받은 사람은
영영 못 받고, 다음 시즌도 열리지 않아 보스러시가 통째로 닫혔습니다.

**논의에서 정한 것.**

| 제시한 의견 | 반영 결과 |
|---|---|
| 배치를 별도 배치서버 한 대만 돌게 분리했으니 '정산 중' 상태는 이제 필요 없지 않나 | 맞습니다. 원래 이 상태는 배치가 여러 대일 때 겹치지 않게 하려던 잠금이었고, 그 역할은 사라졌습니다 |
| 그럼 상태를 없애고 바로 '종료'로 넘긴 뒤, 로그를 보고 수동으로 보상을 다시 주면 되지 않나 | 채택하지 않았습니다. 상태를 없애면 "아직 안 돌았다"와 "돌다가 죽었다"가 DB에서 똑같아 보여 매번 로그를 뒤져야 합니다 |
| **'정산 중'은 그대로 두되, 다시 깨어난 배치가 그걸 보면 "정상 종료되지 않았다"는 뜻으로 받아들여 이어서 하게 하자** | **이 안을 채택했습니다.** 상태의 의미를 '잠금'에서 '정산을 시작했는데 끝내지 못했다는 표식'으로 다시 정의했습니다. DB 한 줄만 봐도 이상을 알 수 있습니다 |

**고친 것.**

1. **깨어나면 '정산 중' 시즌부터 확인**하고, 있으면 경고 로그를 남기고 이어서 정산합니다. 이미 끝난 사람은
   건너뛰도록 되어 있어 같은 작업을 다시 돌려도 안전합니다.
2. **'정산 중' 시즌이 있으면 짧은 간격으로 다시 깨어납니다.** 원래는 시즌이 끝나는 시각까지 자기 때문에
   복구가 최대 일주일 미뤄질 수 있었습니다. *(테스트 중 추가로 발견)*
3. **다음 시즌을 먼저 열고 시즌 종료를 마지막에 찍습니다.** 순서가 반대면 "시즌은 끝났는데 다음 시즌이
   없는" 상태가 생기고, 그때는 아무도 복구하지 못합니다.

```
[WRN] 보스러시 정산 미완료 시즌 감지 — 이어서 정산합니다: seasonId 11
[INF] 보스러시 시즌 정산 완료: seasonId 11 순위 확정 3건 · 보상 발급 3건 → 다음 시즌 12
```

### 6.2 (2.2) — 개별 순위 정산이 실패해도 시즌을 닫음

**문제.** 한 명의 정산이 실패해도 순위 번호는 다음으로 넘어갔고, 실패가 남아 있는데도 시즌을 Closed 형태로 정산이 완료된 것처럼 닫았습니다.
실패한 사람은 순위도 보상도 못 받은 채 시즌이 끝났고, 나중에 재처리하면 이미 쓴 번호와 겹쳤습니다.
또 같은 사람이 계속 실패하면 그 사람을 다시 찾아오길 반복해, 쉬지 않고 DB에 같은 질문을 던졌습니다.

**논의에서 정한 것.** *배치가 계속 실패하는 건 배치가 반복해서 돈다고해서 고칠 수 있는 게 아니라 사람이 봐야 할 일이다* —
자동으로 건너뛰지 않기로 했습니다. 시즌은 '정산 중'에 머물고, 대신 사람이 알아볼 수 있게 로그로 드러냅니다.

**고친 것.**

1. **끝까지 못 돌았으면 시즌을 닫지 않습니다.** '정산 중'으로 남겨 두고 다음번에 이어받습니다.
2. **실패한 자리에서는 순위 번호를 넘기지 않습니다.** 재처리해도 번호가 겹치지 않습니다.
3. **진전이 한 건도 없으면 반복을 멈춥니다.** DB를 쉬지 않고 두드리지 않습니다.
4. 건별 실패 로그와 발화 요약 로그(`… 진전 없음 — 원인 확인이 필요합니다`)를 Error로 남깁니다.

**자동으로 풀 수 있는 상황(재시작·순단)은 배치가 알아서 처리하고, 사람이 봐야 하는 상황은 로그로 드러납니다.**

**고친 파일 (6.1 · 6.2 공통).**

| 파일 | 변경 |
|---|---|
| `BatchServer/Batch/BossRushSeasonBatchScheduler.cs` | 복구 진입점, 미완료 시즌이 있으면 짧은 간격으로 발화, 완주했을 때만 시즌 종료, 진전 없으면 반복 중단, 실패 시 순위 번호 유지, 다음 시즌 개시를 시즌 종료보다 앞으로 |
| `GameServer/Repositories/GameDb/BossRushRepository.cs` (+ 인터페이스) | 정산 중으로 남은 시즌을 찾는 조회 추가 |

### 6.3 (2.3) — 같은 사용자의 값 변경이 겹치면 덮어써짐

**문제.** 골드를 더하고 빼는 코드가 **열한 군데에 같은 모양으로 복사돼** 있었습니다 — ① 잔액을 읽고
② 새 잔액을 계산해 ③ 그 값을 써 넣는 방식입니다. 셋 사이에 틈이 있어 **같은 사람의 요청 둘이 겹치면
양쪽이 같은 잔액을 읽고** 각자 계산한 값을 써 넣으며, 나중에 쓴 쪽이 앞의 결과를 지웁니다. 골드 200인
사람이 60짜리 둘을 동시에 사면 물건은 둘 다 받는데 골드는 60만 빠집니다.

지급에는 문제가 하나 더 있었습니다. 지급 코드가 "재화 행이 없으면 새로 만든다"를 스스로 판단해서, 아직
골드를 받은 적 없는 계정에 지급 둘이 동시에 들어오면 **양쪽이 모두 "행이 없다"를 보고 각자 만들어** 골드가
두 줄로 갈라졌습니다. 조회는 그중 한 줄만 읽으므로 나머지 잔액은 보이지 않게 됩니다. 가능성이 아니라
**이미 일어난 일**이었습니다 — 개발 DB에 한 계정의 골드가 292만·300만 두 줄로 갈라진 채 남아 있었고,
24초 간격으로 만들어진 흔적이 있었습니다(총액을 보존해 병합했습니다).

**논의에서 정한 것.**

| 제시한 의견 | 반영 결과 |
|---|---|
| 검토 문서대로 계정 행을 `FOR UPDATE`로 잠그면, API를 동시에 여러 개 부르는 방치형 특성상 성능 문제가 크지 않나 | 맞습니다. 잠그는 대상을 계정이 아니라 **재화 행 하나**로 좁혔습니다. |
| 재화행이 없는 신규유저에 관해 발생하는 이슈는 **가입할 때 잔액 0인 골드 행을 미리 넣어** 두면 되는 이슈 아닌가 | **이 안을 채택했습니다.** 처음에는 DB에 파생 컬럼과 유니크 제약을 넣었는데, 미리 만들어 두면 지급이 "없으면 만든다"를 판단할 일 자체가 없어집니다. 스키마 변경은 되돌렸습니다. 다만 넣는 시점은 가입이 아니라 **최초 캐릭터 생성(세이브 생성)** 입니다 — 재화 행은 게임 DB에 있고 세이브 행을 참조하므로 그 전에는 넣을 수 없습니다 |

**고친 것.** 겹친 요청을 **거절하지 않고 줄을 세워 순서대로** 처리합니다. 계산에 쓰는 값을 잠금 조회로
읽어 앞 요청이 끝날 때까지 기다렸다가 최신값을 받고, 그 위에서 계산합니다. "읽은 값 그대로일 때만 쓴다"는
조건은 그래도 새는 경우를 잡는 안전망이며, 걸리더라도 실패로 끝내지 않고 다시 실행합니다.

1. **재화는 계산을 앱이 아니라 DB가 합니다.** `잔액 = 잔액 - 비용` 한 문장으로 처리하고, 잔액이 충분한지도
   같은 문장의 조건으로 확인합니다. 읽는 시점과 쓰는 시점이 갈라지지 않고, 부족하면 아무것도 바뀌지 않습니다.
2. **재화 행을 세이브 생성 때 잔액 0으로 미리 만듭니다.** 그 뒤로는 어떤 코드도 재화 행을 만들거나 지우지
   않습니다. 시작 잔액이 0인 것은 신규 지원금을 가입 축하 메일로 주기 때문입니다. 기존 세이브 300건에도
   같은 행을 채웠습니다.
3. **갱신은 줄 번호로 겁니다.** 계정·재화 종류 조건으로 바로 갱신하면 맞는 색인이 없어 그 계정의 아이템
   줄까지 훑으며 잠그고, 잠그는 순서가 엇갈려 교착이 납니다. *(테스트 중 발견 — 메일 9건 동시 수령에서
   6건이 실패했습니다)*
4. **계산에 쓰는 값은 잠금 조회로 읽습니다 — 이것이 주된 동작입니다.** 다른 요청이 그 줄을 잡고 있으면
   **거기서 기다렸다가**, 앞 요청이 끝나면 깨어나 **최신값을 읽고** 이어서 진행합니다. 그래서 겹친 요청이
   차례로 전부 처리됩니다.

   ```
   요청 A: 줄 잠금 → 값 읽음(100) → 계산 → 갱신 → 커밋
   요청 B:        ↳ 여기서 대기 ───────────────↑ → 값 읽음(101) → 계산 → 갱신 → 커밋
   ```

   잠금 없이 읽으면 이렇게 되지 않습니다. **트랜잭션이 처음 읽은 시점의 사진**을 끝까지 보여 주기 때문에,
   다른 요청이 값을 바꿔 커밋했어도 옛 값이 그대로 보입니다. *(테스트 중 발견 — 이걸 넣기 전에는 동시 8건 중
   3건만 성공하고 나머지가 오류로 떨어졌습니다)*
5. **갱신은 "읽은 값 그대로일 때만" 합니다 — 안전망입니다.** 4번이 이미 줄을 세웠으니 이 조건은 사실상 항상
   맞습니다. 잠금을 거치지 않은 경로가 그 줄을 건드린 경우에만 어긋나고, 그때도 거절하지 않고 **트랜잭션을
   처음부터 다시 실행**합니다(최대 3회). 새 트랜잭션은 사진도 새로 찍으므로 최신값을 다시 읽습니다.
6. **가방 칸은 이미 있는 "한 칸에 한 줄" 제약에 맡깁니다.** 빈 칸 조회도 잠금 조회로 바꿔 애초에 겹치지
   않게 하고, 그래도 부딪히면 트랜잭션을 다시 실행해 다음 칸을 받습니다.

**사용자에게 실패로 나가는 것.** 경합 때문에 거절되는 요청은 없습니다. 실패로 응답하는 건 **원래 실패해야
하는 것들**뿐입니다 — 잔액 부족, 이미 깬 스테이지를 프런티어로 다시 깨려는 요청(진입 상태가 아님) 등.
세 번을 다시 실행해도 계속 부딪히면 그때는 오류로 올리는데, 이는 경합이 아니라 다른 결함을 의심할 상황입니다.

**잠그는 범위.** 계정 전체가 아니라 **그 요청이 실제로 만지는 줄**만 잠급니다. 골드를 만지면 골드 줄,
경험치를 주면 파티 캐릭터 줄, 가방에 넣으면 그 계정의 가방 칸입니다. 그래서 세이브 불러오기·파티 편성처럼
그 값을 만지지 않는 요청은 줄을 서지 않습니다.


**고친 파일.**

| 파일 | 무엇을 고쳤나 |
|---|---|
| `GameDbBase.cs` | 경합 신호와 **트랜잭션 다시 실행**(교착·칸 선점 실패 포함) |
| `Constants.cs` | 다시 실행 횟수, 교착·잠금 대기 오류 번호 |
| `TradeRepository.cs` | 거래소 구매 골드 · 아이템 합치기 |
| `GachaRepository.cs` | 뽑기 비용 · 천장 카운터 · 아이템 합치기 |
| `GrowthRepository.cs` | 룬 강화 비용·레벨 · 스킬 레벨 |
| `InventoryRepository.cs` | 장비 강화 비용·단계 · 가방 확장 비용·용량 |
| `SaveRepository.cs` | 캐릭터 추가 비용 · **재화 행 미리 만들기** |
| `CubeRepository.cs` | 제작 비용 · 분해 보상 · 큐브 상태 · 재료 차감 · 아이템 합치기 |
| `StageRepository.cs` | 클리어 골드·경험치 · 진행도 전진 · 전리품 합치기 |
| `MailRepository.cs` | 첨부 골드 · 첨부 아이템 합치기 |
| `OfflineRepository.cs` | 방치 골드·경험치 |
| `InventorySlotAllocator.cs` | 빈 칸 조회를 잠금 조회로 |

### 6.4 (2.4) — 보스러시 동점자 순서가 정해져 있지 않음

**문제.** 기록이 밀리초까지 같고 기록 시각(`recorded_at`)까지 같은 두 사람이 있으면 누가 위인지 정해
두지 않아, 순위를 보여 주는 세 경로(Redis 실시간 랭킹 · MySQL 랭킹 · 시즌 정산)가 서로 다른 답을 낼 수
있었습니다. 확률은 매우 낮지만 확정 순위는 한 번 정해지면 바뀌지 않으므로 기준은 필요했습니다.

**논의에서 정한 것.**

| 제시한 의견 | 반영 결과 |
|---|---|
| MySQL에 `user_id`를 추가하고 Redis에도 정렬 기준을 하나 더 넣으면 되지 않나 | 의견을 채택하여 우선순위를 부여하도록 변경하였습니다 |

**고친 것.** **동점이면 `user_id`가 작은 쪽을 위로** 두기로 정하고 세 경로에 같은 기준을 적용했습니다.

- **MySQL**: 정렬 조건 맨 뒤에 `user_id`를 추가했습니다(랭킹 조회 2곳 · 정산 1곳).
- **Redis**: 사용자 번호를 **자릿수를 맞춘 문자열**로 저장합니다. Redis는 점수가 같으면 글자 순서로
  비교하는데, 자릿수가 다르면 `1000, 120, 9` 처럼 어긋납니다. `9`를 `00000000000000000009` 로 채우면
  글자 순서가 곧 숫자 순서가 되어 MySQL과 같은 기준이 됩니다.
- 형식이 바뀌므로 예전 형식이 섞이지 않게 **순위표를 지우고 관리 API로 다시 채웁니다**
  (`POST /api/admin/boss-rush/rank/warmup?force=true`). 순위표는 게임 DB에서 언제든 다시 만들 수 있는
  임시 데이터입니다.

**확인.** 점수가 같은 항목 `9`·`120`·`1000`의 순서가 숫자 순서와 일치했고, 순위표를 다시 채운 뒤(151명)
상위 순서가 게임 DB 정렬 결과와 한 명씩 그대로 일치했습니다. 랭킹 조회 API 응답도 정상입니다.

**고친 파일.** `BossRushRankCache.cs`(저장·조회 형식), `BossRushRepository.cs`(정렬 3곳),
`Constants.cs`(자릿수 상수 20).

### 6.5 (2.5) — `ServerError`가 HTTP 400으로 나감

**문제.** 분류되지 않은 에러 코드를 전부 400으로 처리하고 있어서, 서버 결함인 `ServerError`까지 클라이언트
잘못으로 나갔습니다.

**고친 것.** 예외 처리 정비 작업(서비스 로직 전체를 예외로부터 감싸고 에러 코드마다 로그를 남기도록 한
피드백) 중에 함께 반영해, 두 서버의 베이스 컨트롤러에서 `ServerError`를 **HTTP 500**으로 매핑했습니다.

### 6.6 (2.6) — 요청 배열의 `null` 요소가 HTTP 500을 만듦

**문제.** 요청 body의 목록 요소는 참조형이라 `{ "members": [null] }` 같은 body가 그대로 바인딩됩니다.
서비스가 요소를 그대로 읽으면 `NullReferenceException`이 납니다.

**논의에서 정한 것.**

| 제시한 의견 | 반영 결과 |
|---|---|
| 그 예외는 `GlobalExceptionHandler`가 잡아 주지 않나 | 이미 잡히고 있었습니다 — 세 서비스가 각자 `catch (Exception)`으로 감싸 `ServerError`로 바꾸고, 전역 처리기까지 가더라도 결과는 같은 **500**입니다. **문제는 잡히느냐가 아니라 무엇으로 분류되느냐**였습니다. 잘못된 요청이 서버 결함으로 나가면 클라이언트는 재시도할 값으로 보고, Error 로그와 `api_error` 집계 이벤트가 매번 쌓여 모니터링에 서버 장애로 잡힙니다 |
| 그러면 전역 처리기에서 `NullReferenceException`을 400으로 매핑하면 되지 않나 | 채택하지 않았습니다. 진짜 서버 결함인 `NullReferenceException`까지 전부 클라이언트 잘못으로 분류되어, 6.5에서 `ServerError`를 500으로 바로잡은 것을 되돌리는 셈입니다 |
| 요청 검증 계층(필터)을 두어 목록의 `null` 요소를 한 번에 거르자 | **한 번 만들었다가 걷어냈습니다.** 목록을 받는 요청은 셋뿐이고 셋 모두 이미 형식 검증 함수를 갖고 있는데, 그것을 위해 리플렉션으로 요청 객체를 훑는 횡단 계층을 두는 것은 문제 크기에 비해 과했습니다. 게다가 그 필터는 `List<T>`만 보므로 배열 필드가 생기면 그대로 통과시키면서 "검증 계층이 다 막아 준다"는 잘못된 안전감만 남깁니다 |
| **각 요청의 형식 검증에 요소 `null` 검사를 넣자** | **이 안을 채택했습니다.** 개수·범위·중복을 보던 자리에 한 줄을 더하는 것이라, 검사가 코드를 읽는 사람에게 그대로 보입니다 |

**고친 것.** 목록을 받는 세 요청의 **형식 검증에서 요소가 `null`이면 `InvalidRequest(1006)`으로 거절**합니다.
셋 다 DB에 닿기 전이고, 값 타입 목록(`List<long>` 등)은 애초에 `null` 요소가 들어올 수 없어 대상이 아닙니다.

| 요청 | 검사 위치 |
|---|---|
| `party/arrange` | `SaveService.ValidatePartySnapshot` — 자리·중복 검사 앞 |
| `cube/dismantle` | `CubeService.DismantleAsync` — 목록을 `(itemId, count)`로 바꾸기 전 |
| `boss-rush/clear` | `BossRushService.ClearAsync` — 라운드 정합성 검사 앞(`runId`·`clearMs` 형식 검사와 같은 자리) |

**확인.** 두 서버를 띄우고 실제로 호출했습니다.

| 요청 | 결과 |
|---|---|
| `party/arrange` `members: [null]` · `[{...}, null]` | 400 · `InvalidRequest(1006)` |
| `cube/dismantle` `items: [null]` | 400 · `InvalidRequest(1006)` |
| `boss-rush/clear` `rounds: [null]` | 400 · `InvalidRequest(1006)` |
| `create-character` · `load` · `party/arrange`(정상 1명) | 200 — 정상 요청은 영향 없음 |
| `party/arrange` `members: []` · `cube/dismantle`(없는 아이템) · `boss-rush/clear`(형식 정상) | 기존 검증 그대로(2008 · 4001 · 13006) |

**고친 파일.** `SaveService.cs` · `CubeService.cs` · `BossRushService.cs`.

### 6.7 (2.7) — 닉네임 길이 검증이 없음

**문제.** 비어 있는지만 확인하고 값을 그대로 DB에 넘겨서, `VARCHAR(50)`을 넘기는 닉네임이 오면 저장
단계에서 오류가 나거나 문자열이 조용히 잘렸습니다.

**논의에서 정한 것.** 길이 상한을 컬럼 크기(50)가 아니라 **클라이언트 입력 UI가 막는 길이(12자)** 에
맞췄습니다. 서버가 컬럼 크기까지 받아 주면 클라이언트와 서버가 서로 다른 규칙을 갖게 되고, 그때 들어온
값은 정상 입력이 아니라 검사를 우회한 요청입니다. 또 **비어 있는 것과 긴 것은 다른 실패**라서, 길이 초과는
전용 코드 `NicknameTooLong(1007)`으로 내려 클라이언트가 "닉네임이 깁니다"를 그대로 안내할 수 있게 했습니다
(빈 값은 형식 오류라 `InvalidRequest(1006)`).

**고친 것.**

1. **닉네임을 받는 두 경로에서 모두 검사합니다.** 회원가입(`POST /api/auth/signup`)과 최초 캐릭터 생성
   (`POST /api/game/create-character`)이 같은 규칙을 씁니다.
2. **DB 트랜잭션을 시작하기 전에 거절합니다.** `Trim()`한 길이로 검사하므로 공백만 채운 값도 걸립니다.
3. **상한은 상수로 두고 두 서버가 같은 값을 씁니다** — `GameServer` `Constants.Player.NicknameMaxLength`,
   `AccountServer` `Constants.Auth.MaxNicknameLength` 모두 12입니다. 한쪽만 바뀌면 두 경로의 규칙이
   어긋나므로 상수 주석에 서로를 적어 두었습니다.

**고친 파일.** `GameServer/Services/SaveService.cs`·`GameServer/Constants.cs`,
`AccountServer/Services/AuthService.cs`·`AccountServer/Constants.cs`,
`TaskbarHero.Common/ErrorCode.cs`(1007) 및 두 서버의 베이스 컨트롤러 메시지 매핑.
문서는 `docs/공통/api-통합.md` · `docs/공통/error-code-정의.md` ·
`docs/세부/account-login-기획서.md` · `docs/세부/save-data-기획서.md`에 반영돼 있습니다.

---

## 7. 실패 처리 재검토 — 서비스·리포지토리

- 검토 일시: 2026-08-27 KST
- 검토 범위: `GameServer/Services/` · `GameServer/Repositories/`(GameDb·MasterDb·MemoryDb) ·
  `AccountServer/Services/` · `AccountServer/Repositories/`(AccountDb·MemoryDb)
- 검토 방식: 정적 코드 분석. **코드에서 확인한 사실만 적는다** — 서버를 띄운 실행 검증은 하지 않았으므로,
  항목마다 **지금 실제로 도달하는 경로**인지 **아직 도달하지 않는 잠재 결함**인지를 표시한다.
- 2장과 겹치지 않는 것만 담는다. 여기서 보는 축은 하나다 — **실패했을 때 그 사실을 누구에게 어떻게 알리는가.**

### 7.0 먼저, 이미 갖춰져 있는 것

아래 항목들은 계층 전체에서 일관되게 지켜지고 있어 이 검토의 대상이 아니다(재확인만 했다).

- 서비스의 **모든 공개 메서드가 본문 전체를 `try`로 감싸고**, 잡은 예외를 Error로 남긴 뒤 `ServerError`를 돌려준다.
  응답에 내부 정보를 싣지 않는다.
- 리포지토리의 상태 변경은 전부 `GameDbBase.TransactionAsync` + `TxResult`를 거치므로 **검증 실패에서 롤백이 빠질 수 없다.**
- 경합(`ConcurrencyConflictException`·교착·잠금 대기 초과·유니크 선점)은 트랜잭션째 재실행된다(6.3).
- Redis 계층은 **정본이냐 파생이냐로 실패 정책이 갈린다** — 파생(랭킹·닉네임·시즌 메타)은 `SafeAsync`로 흡수하고,
  정본(인증 토큰)은 예외를 그대로 올려 요청을 거부한다.
- 마스터 적재는 가챠 배너를 **검증해 통과한 것만 담고**, 탈락 사유를 Error로 남긴다.
- 큐브 분해는 수량 하한(`count < 1`)을 리포지토리에서 막아, 음수 수량으로 아이템이 늘어나는 경로가 없다.

### 7.1 요약

| 항목 | 심각도 | 무슨 일이 일어나나 | 언제 일어나나 |
|---|---|---|---|
| [7.2](#72-높음--재화-행이-없을-때-차감은-사용자-실수로-지급은-서버-결함으로-갈린다) | 높음 | 골드가 있는데도 "골드가 부족합니다"로 거절되고, **서버에는 아무 기록도 남지 않아** 아무도 알아채지 못한다 | 재화 행이 사라진 계정이 무언가를 구매·강화할 때 |
| [7.3](#73-높음--커밋-뒤의-이벤트-로그-방출이-성공한-요청을-500으로-만든다) | 높음 | 보상은 이미 들어갔는데 응답은 실패(500)로 나가고, 클라이언트가 같은 요청을 다시 보낸다 | 이벤트 로그를 남기는 데 실패했을 때 |
| [7.4](#74-높음--랭킹-캐시가-쓸-수-없는-것과-기록이-없는-것을-구분하지-않는다) | 높음 | 기록이 있는 유저에게 "기록 없음"을, 랭킹 조회에는 빈 목록을 정상 응답으로 내려준다 | 순위표(Redis)를 비우고 다시 채우기 전 구간 |
| [7.5](#75-높음--계정-서버의-두-저장소-부분-실패에-보상이-없다) | 높음 | 로그아웃이 실패했다고 응답하면서 **세션은 그대로 살아 있다.** 같은 계정으로 동시에 로그인하면 정상 로그인이 서버 오류가 된다 | Redis가 잠깐 죽었을 때 · 로그인 요청이 겹칠 때 |
| [7.6](#76-중간--소모품-사용만-경합을-사용자-실수로-되돌린다) | 중간 | 소모품을 연달아 쓰면 한 번은 "아이템이 없습니다"로 거절된다(다른 기능은 겹쳐도 순서대로 처리된다) | 같은 소모품 사용 요청이 겹칠 때 |
| [7.7](#77-중간--실패-상태-매핑이-닫혀-있지-않다) | 중간 | 리포지토리가 새로운 실패 사유를 돌려주면, 그 실패가 **성공으로 응답되거나 500으로 터진다** | 지금은 없음 — 실패 사유를 하나 추가하는 순간 |
| [7.8](#78-중간--마스터-적재-실패를-흡수하고-복구-경로가-없다) | 중간 | 스테이지·가챠·큐브가 전부 "콘텐츠 없음"이 되고, **서버를 다시 띄우기 전까지 회복되지 않는다**(헬스 체크는 정상) | 기동할 때 마스터 DB를 읽지 못했을 때 |
| [7.9](#79-중간--오프라인-정산과-하트비트가-같은-컬럼을-두고-경쟁한다) | 중간 | 받지 못한 방치 보상이 "이미 받았습니다"로 거절되고, 다시 요청해도 **정산이 사라진 상태가 된다** | 접속 직후 하트비트와 정산 요청이 겹칠 때 |
| [7.10](#710-낮음--회원가입-입력의-길이-상한을-검사하지-않는다) | 낮음 | 너무 긴 이메일이 "잘못된 요청"이 아니라 서버 오류(500)로 거절된다(닉네임만 6.7에서 막았다) | 255자를 넘는 이메일로 회원가입할 때 |
| [7.11](#711-낮음--그-밖에-확인한-것들) | 낮음 | 조치 전 상태를 설명하는 낡은 주석 등, 지금 당장 오작동은 아니지만 나중에 결함이 될 5건 | 코드를 읽거나 수량·동시 요청 조건이 바뀔 때 |

### 7.2 (높음) — 재화 행이 없을 때 차감은 사용자 실수로, 지급은 서버 결함으로 갈린다

**무엇이 문제인가.** 6.3에서 **"재화 행은 세이브를 만들 때 잔액 0으로 함께 만들고 어디서도 지우지 않는다"**를
불변식으로 정했다. 그런데 그 불변식이 깨졌을 때의 처리가 방향에 따라 정반대다.

| 방향 | 재화 행이 없으면 | 사용자가 보는 것 | 운영자가 보는 것 |
|---|---|---|---|
| 지급 `CreditCurrencyAsync` | `InvalidOperationException`을 던진다 | 500 · `ServerError` | Error 로그 1줄 |
| 차감 `TryDebitCurrencyAsync` | `null`을 돌려준다 (= 잔액 부족과 같은 값) | 400 · `InsufficientCurrency` | **아무것도 없다** |

**근거.**

- `GameServer/Repositories/GameDb/MailRepository.cs:528` — 지급 쪽의 `throw`(같은 코드가 Stage·Offline·Cube에 복제).
- `GameServer/Repositories/GameDb/SaveRepository.cs:565` — 차감 쪽의 `return null`(같은 코드가 Gacha·Growth·Inventory·Cube·Trade에 복제).
- 같은 파일 주석이 이 거동을 의도로 적어 두었다 — "재화 행이 없는 계정은 잔액 0으로 취급돼 같은 경로로 거부된다".

**왜 문제인가.** 차감 경로에서 불변식이 깨지면 **골드를 가진 사용자가 "골드가 부족합니다"를 보고, 서버에는 로그가
한 줄도 남지 않는다.** 6.3에서 실제로 겪은 결함(재화 행이 두 줄로 갈라져 잔액 절반이 보이지 않던 계정)이 바로
이 형태였고, 그때 문제를 찾아낸 것은 로그가 아니라 사람의 눈이었다. 지급 쪽에만 감지 장치를 달아 둔 상태다.

**권장 방향.** 차감도 "행이 없다"와 "잔액이 모자라다"를 **다른 값으로 구분**해 돌려주고, 앞쪽은 지급과 같이
서버 결함(Error 로그 + `ServerError`)으로 올린다. 헬퍼가 9개 파일에 복제돼 있으므로 한곳으로 모으는 것이
같은 작업이다(복제된 채로는 한쪽만 고쳐질 수 있다).

### 7.3 (높음) — 커밋 뒤의 이벤트 로그 방출이 성공한 요청을 500으로 만든다

**무엇이 문제인가.** 서비스들은 규약대로 **트랜잭션이 커밋된 뒤에** 이벤트 로그를 방출한다(롤백된 사실을 로그에
남기지 않기 위해서다, 로그 이벤트 정의 4.2). 그런데 그 호출이 **공개 메서드 전체를 감싼 같은 `try` 블록 안에** 있고,
`EventLogger.Action`에는 방어가 없다.

**근거.**

- `GameServer/Logging/EventLogger.cs:48` — `Action`에 `try`/`catch`가 없다. 필드 직렬화(`JsonSerializer`)와
  로거 sink 호출이 그대로 호출자에게 예외를 전파한다.
- 커밋 뒤 방출 지점: 스테이지 클리어·오프라인 정산·메일 수령(단건/일괄)·거래 등록/구매/취소·가챠·큐브 3종·
  강화·확장·출석·캐릭터 생성 — 서비스 10곳이 모두 같은 배치다.

**왜 문제인가.** DB는 이미 반영됐는데 클라이언트는 실패(500)를 받는다. **방치형 클라이언트는 실패한 요청을
다시 보내므로**, 스테이지 클리어·메일 수령·거래 구매가 두 번 처리될 수 있다(각 경로에 중복 방지 게이트가 있어
자산이 두 배가 되지는 않지만, 사용자는 "성공했는데 실패로 보이는" 상태를 겪는다). 로그를 남기지 못한 것이
**보상 지급을 취소할 이유는 아니다.**

**권장 방향.** 이벤트 로깅은 실패를 흡수해야 한다 — `Action` 안에서 잡아 Warning으로 남기고 요청은 성공으로
끝낸다(Redis 파생 캐시에 `SafeAsync`를 둔 것과 같은 판단이다). 부수적으로 `EventLoggerExtensions.EmitItemFlow`는
장비 수량만큼 루프를 돌아 로그 행을 내므로, 수량 값이 비정상인 데이터가 들어오면 그대로 로그 폭주가 된다.

### 7.4 (높음) — 랭킹 캐시가 "쓸 수 없는 것"과 "기록이 없는 것"을 구분하지 않는다

**무엇이 문제인가.** `BossRushRankCache`는 모든 접근을 `SafeAsync`로 감싸 **실패와 부재를 똑같이 null·기본값으로**
돌려준다. 호출측이 그 값을 폴백 신호로 쓰는데, 두 경우가 구별되지 않아 판정이 어긋난다.

**근거.**

- `GameServer/Services/BossRushService.cs:376` — `GetMyRankAsync`는 **`total is null`만** 폴백 조건으로 쓴다.
  `CountAsync`는 키가 없어도 0을 돌려주므로(실패만 null), **리더보드가 아직 적재되지 않았으면 폴백이 일어나지 않고**
  `GetMyEntryAsync`의 null이 곧 "기록 없음"으로 확정된다 → `myRank: null` · `totalEntries: 0`.
- `GameServer/Services/BossRushService.cs:320` — `GetRankAsync`는 `cached is null || total is null`로 조건이 하나 더 있지만,
  키가 없을 때는 **빈 목록 + 0**이 정상값으로 내려와 역시 폴백하지 않는다 → 빈 랭킹을 성공으로 응답한다.
- `GameServer/Services/BossRushService.cs:256` — 클리어 보고 뒤의 `_rankCache.UpsertAsync` **반환값(false = 반영 실패)을
  버린다.** 그러고는 이어서 읽은 순위를 응답에 담으므로, 캐시 반영이 실패하면 갱신 전 순위가 "새 기록의 순위"로 나간다.
  같은 false를 워밍업 서비스는 실패로 세어 상태를 `CacheUnavailable`로 내리는데(`BossRushRankWarmupService`),
  조회·보고 경로는 세지 않는다.

**왜 문제인가.** 6.4에서 랭킹 형식을 바꾸며 **"순위표를 지우고 관리 API로 다시 채운다"**를 절차로 정했다.
즉 **리더보드가 비어 있는 구간이 운영 절차에 실재한다.** 그 구간에 들어온 조회는 폴백 대상인데 정상 응답으로
"당신은 기록이 없습니다"를 받는다. 캐시는 파생이므로 응답이 정본과 달라지면 축소 운전이 아니라 오답이다.

**권장 방향.** 캐시 계층이 **실패와 부재를 다른 값으로** 알리게 하고(예: 실패는 null, 부재는 빈 결과),
호출측은 실패에서만 MySQL로 폴백한다. 클리어 보고의 반영 실패는 최소한 Warning으로 남기고,
그 응답의 순위가 확정값이 아님을 알 수 있게 한다.

### 7.5 (높음) — 계정 서버의 두 저장소 부분 실패에 보상이 없다

**무엇이 문제인가.** 세션의 정본은 Redis이고 MySQL `user_auth_token`은 영속 백업이다. 그런데 쓰기 순서가
그 관계를 따르지 않고, 한쪽만 성공한 상태를 되돌리는 처리도 없다.

**근거.**

- `AccountServer/Services/AuthService.cs:175` — 로그아웃이 **MySQL 행 삭제 → Redis 키 삭제** 순서다.
  Redis 삭제가 실패하면 500을 돌려주지만 **정본인 Redis 토큰은 살아 있어 세션이 그대로 유효하다** —
  사용자는 로그아웃에 실패했다고 안내받고, 실제로도 로그아웃되지 않았지만, DB의 영속 백업만 사라진다.
- 같은 파일 로그인 경로 — **MySQL UPSERT → Redis SET** 순서다. Redis 쓰기가 실패하면 DB에는 새 토큰이,
  Redis에는 옛 토큰이 남아 **이전 세션이 계속 통과한다**(단일 세션 정책이 깨진다).
- `AccountServer/Repositories/AccountDb/AuthTokenRepository.cs:29` — UPSERT가 `UPDATE` → 0행이면 `INSERT`인
  **두 문장이고 트랜잭션이 없다**(주석도 그 위험을 적어 두었다). 동시 로그인이면 두 `INSERT`가 겹쳐 PK 중복 예외가
  나고, `LoginAsync`의 마지막 `catch`가 그것을 **정상 로그인의 500 `ServerError`로** 만든다.
  GameDb에는 같은 상황을 재실행으로 흡수하는 장치(`GameDbBase.IsConflict` — `DuplicateEntry` 포함)가 있는데
  AccountDb에는 없다(`AccountDbBase`에 트랜잭션·재실행 헬퍼를 두지 않았다).

**권장 방향.** 세션 무효화는 **정본(Redis)을 먼저** 지운다(백업이 남는 것은 다음 로그인 UPSERT가 덮는다).
UPSERT는 한 문장으로 만들거나, `DuplicateEntry`를 잡아 UPDATE로 재시도해 정상 로그인이 500이 되지 않게 한다.

### 7.6 (중간) — 소모품 사용만 경합을 사용자 실수로 되돌린다

**무엇이 문제인가.** 6.3에서 **"경합 때문에 거절되는 요청은 없다 — 겹친 요청은 줄을 세워 순서대로 전부 처리한다"**를
규칙으로 정하고, CAS가 0행이면 `ConcurrencyConflictException`을 던져 트랜잭션째 재실행하도록 통일했다.
소모품 사용만 그 규칙 밖에 있다.

**근거.**

- `GameServer/Repositories/GameDb/ConsumableRepository.cs:121` — 수량 차감 CAS가 0행이면 예외가 아니라
  `ConsumableUseStatus.ItemNotFound`를 **정상 반환**한다 → 사용자는 "아이템이 없습니다"(`ItemNotFound`)를 받는다.
  재실행 대상이 아니므로 재시도도 없다.
- 같은 CAS를 Stage·Cube·Gacha·Growth·Inventory·Mail·Trade는 예외로 올린다(`ConcurrencyConflictException`을 쓰는
  파일 8개 대비 이 파일 0건).
- 대상 행을 **잠금 없이** 읽는다. 같은 파일 주석은 "행 잠금(FOR UPDATE)은 다른 리포지토리와 동일하게 쓰지 않는다"고
  적고 있으나, **6.3 이후 다른 리포지토리는 계산 입력을 잠금 조회로 읽는다** — 주석이 현재 코드와 어긋나고,
  잠금 없는 스냅샷 읽기 때문에 겹친 요청의 CAS가 예측 가능하게 어긋난다.

**권장 방향.** 다른 경로와 같은 규약으로 맞춘다 — 차감 대상 행을 잠금 조회로 읽고, CAS 0행은 경합 신호로 올려
재실행한다. 낡은 주석도 함께 고친다.

### 7.7 (중간) — 실패 상태 매핑이 닫혀 있지 않다

**무엇이 문제인가.** 리포지토리 상태(`*Status`)를 에러 코드로 바꾸는 코드가 두 형태로 공존한다.

| 형태 | 위치 수 | 알려지지 않은 상태가 오면 |
|---|---|---|
| `switch` **문**, `default` 없음 | 21곳 (`switch (outcome.Status)`) | 아무 분기도 타지 않고 **성공 경로로 흐른다** |
| `switch` **식**, `_ => ServerError` | 2곳 (`MailService`·`ConsumableService`의 `ToErrorCode`) | `ServerError`로 닫힌다 |

**근거(가장 구체적인 자리).** `TradeCloseStatus` 하나를 구매와 취소가 **함께 쓴다.**

- `GameServer/Services/TradeService.cs:207` 구매 — `NotOwner`·`InventoryFull`을 처리하지 않는다.
- `GameServer/Services/TradeService.cs:292` 취소 — `SelfPurchase`·`InsufficientGold`를 처리하지 않는다.
- 두 경로 모두 `switch` 직후에 `outcome.Listing!`(null 강제 역참조)로 진행한다(`TradeService.cs:224`·`308`).
  실패 결과는 `Listing`이 null이므로 **`NullReferenceException` → 500**이 된다.

**지금 드러나는가.** 아니다 — 현재 `TradeRepository`는 구매에서 그 두 값을, 취소에서 다른 두 값을 돌려주지 않는다
(`TradeCloseStatus.` 반환 지점 전수 확인). **한쪽에 상태를 하나 추가하는 순간** 상대 쪽이 조용히 성공으로 흐른다.

**권장 방향.** 상태→에러코드 변환을 전부 `switch` 식(`_ => ServerError` + Error 로그)으로 통일한다.
`ServerError`는 6.5에서 이미 HTTP 500으로 매핑돼 있으므로, 닫는 것만으로 "모르는 실패"가 서버 결함으로 드러난다.

### 7.8 (중간) — 마스터 적재 실패를 흡수하고 복구 경로가 없다

**무엇이 문제인가.** `MasterDbProvider.LoadAsync`는 적재 실패를 잡아 `IsLoaded=false`로 두고 **기동을 계속한다.**
호출자는 `Program.cs` 한 곳(기동 시 1회)뿐이라 **재적재 진입점이 없다.**

**근거.**

- `GameServer/Repositories/MasterDb/MasterDbProvider.cs:467` — `catch`에서 `IsLoaded=false` + Error 로그로 끝난다.
- `GameServer/Program.cs:218` — 유일한 호출. 실패해도 `app.Run()`으로 진행한다.
- 필드 대입이 **필수 마스터 검증(`_classes.Count == 0`)보다 앞에** 있어, 검증에 걸린 스냅샷이 그대로 남는다.
- `IsLoaded`를 확인하지 않는 엔드포인트가 있다 — 가방 페이지 조회·장착 해제·배치 이동, 메일 목록,
  거래 목록/취소, 활성 버프 조회. 마스터가 필요 없어 의도된 것이지만, 결과적으로 **장애가 일부 API에서만 드러난다.**

**왜 문제인가.** 기동 시 MySQL이 잠깐 늦게 뜨면(compose 기동 순서·순단) 그 프로세스는 **재시작 전까지 영구히
축소 운전**한다. 스테이지·가챠·큐브·성장은 전부 `MasterDataNotLoaded(10001)`가 되는데, 이는 사용자에게
"콘텐츠가 없는 것"처럼 보이고 헬스 체크(`/openapi/v1.json`)는 정상으로 응답한다.

**권장 방향.** 적재 실패를 기동 실패로 다루거나(부트스트랩이 재시도할 수 있다), 워밍업처럼
**관리 API로 재적재할 수 있는 진입점**을 둔다(랭킹 워밍업이 이미 같은 형태를 쓴다). 필드 대입은 검증 뒤로 옮긴다.

### 7.9 (중간) — 오프라인 정산과 하트비트가 같은 컬럼을 두고 경쟁한다

**무엇이 문제인가.** 오프라인 정산은 `game_player.last_active_at`의 CAS로 정산권을 선점한다. 그런데
**같은 컬럼을 하트비트가 갱신한다**(`SaveRepository.UpdateLastActiveAsync` — heartbeat 엔드포인트).

**근거.** `GameServer/Repositories/GameDb/OfflineRepository.cs:110` — 관측값과 다르면 0행 → `AlreadyClaimed`를
**정상 반환**한다(예외가 아니므로 트랜잭션 재실행 대상이 아니다). 관측(1단계)과 CAS(2단계) 사이에 하트비트가
끼면 이 경로를 탄다.

**왜 문제인가.** 사용자는 **받지 못한 보상을 "이미 수령했습니다"(`OfflineRewardAlreadyClaimed`)로 안내받는다.**
게다가 하트비트가 `last_active_at`을 밀어 놓았으므로 다시 요청해도 경과가 최소 기준 미만이라 `NoOfflineReward`가 된다 —
**정산 자체가 사라진다.** 앱 진입 시 하트비트와 정산 요청이 함께 나가는 것이 정상 흐름이라 확률이 낮지 않다.

**권장 방향.** 정산권 선점을 하트비트와 다른 값으로 분리하거나(정산 전용 컬럼), CAS 실패를 경합 신호로 올려
재실행한다(재실행하면 새 관측값으로 다시 판정하므로 경과가 남아 있으면 정산된다).

### 7.10 (낮음) — 회원가입 입력의 길이 상한을 검사하지 않는다

6.7에서 닉네임 길이를 막았지만 **같은 종류의 구멍이 두 개 남아 있다.**

- **이메일**: `AccountServer/Services/AuthService.cs:296`의 `IsValidEmail`은 형식만 본다
  (`MailAddress.TryCreate`). `users.email`은 `VARCHAR(255)`(`docs/공통/db-schema.sql:44`)이므로,
  255자를 넘는 주소는 선검사(`SELECT`)를 통과한 뒤 `INSERT`에서 MySQL 오류가 되고, 그 예외는
  `DuplicateEntry`가 아니라서 마지막 `catch`로 떨어져 **500 `ServerError`**가 된다. 6.7 이전의 닉네임과 같은 형태다.
- **비밀번호**: 최소 6자만 본다(`Constants.Auth.MinPasswordLength`). 상한이 없어 아주 긴 입력이 그대로
  BCrypt로 넘어가고, bcrypt는 규격상 **앞 72바이트만** 해시 입력으로 쓴다(`BCrypt.Net-Next 4.0.3`).

**권장 방향.** 닉네임과 같은 자리에서 이메일·비밀번호의 최대 길이도 검사해 **DB에 닿기 전에** 거절한다
(상한은 `AccountServer/Constants.cs`에 둔다). 이메일 길이 초과는 형식 오류이므로 `InvalidRequest`로 충분하다.

### 7.11 (낮음) — 그 밖에 확인한 것들

| 위치 | 확인한 사실 |
|---|---|
| `StageRepository.ApplyClearAsync` 주석 | 경고 블록이 **6.3 조치 이전 상태**("행에 잠금을 걸지 않으므로 … 백로그 과제")를 그대로 설명한다. 실제 코드는 CAS + 잠금 조회로 고쳐져 있다 — 읽는 사람이 미조치로 오인한다 |
| `StageRepository.StoreDroppedItemAsync` | 스택 병합이 `quantity < stack_max` 행을 골라 **합계가 `stack_max`를 넘는지 확인하지 않는다.** 현재 드롭 수량이 항상 1이라 드러나지 않지만, 수량 2 이상 드롭이 생기면 상한을 넘긴다 |
| `SaveRepository.SavePartyAsync` | 보유 캐릭터를 **잠금 없이** 읽고, 자리 부여 `UPDATE`의 영향 행 수를 확인하지 않는다. 스냅샷 덮어쓰기라 마지막 요청이 이기지만, 겹친 요청의 **응답이 다른 요청의 결과를 담을 수 있다**(마지막 단계에서 다시 읽는다) |
| `BossRushRepository.ApplyClearAsync` 5) | 시즌 기록 `INSERT`가 유니크 위반이면 트랜잭션째 재실행되는데, 재실행 시점에는 런이 이미 `Cleared`라 **정상 클리어가 `AlreadyFinished`로** 응답된다 |
| `GachaService.GetBannersAsync` | 배너마다 천장 카운터를 따로 조회한다(N+1). 실패 처리는 아니지만 배너 수에 비례해 지연이 늘고, 한 배너의 조회 실패가 목록 전체를 `ServerError`로 만든다 |

### 7.12 권장 조치 순서

**P0 — 사용자에게 잘못된 사실을 알리는 것부터**

1. 7.4 랭킹 캐시의 부재/실패 구분(운영 절차상 비어 있는 구간이 실재한다).
2. 7.2 재화 행 부재를 차감 경로에서도 서버 결함으로 드러내기.
3. 7.3 이벤트 로깅 실패를 요청 실패로 만들지 않기.

**P1**

4. 7.5 계정 서버 세션 무효화 순서 · 동시 로그인 UPSERT.
5. 7.9 오프라인 정산권을 하트비트와 분리.
6. 7.6 소모품 경합을 다른 경로와 같은 규약으로.

**P2**

7. 7.7 상태 매핑을 `switch` 식으로 통일.
8. 7.8 마스터 재적재 경로.
9. 7.10 입력 길이 상한 · 7.11의 낡은 주석과 잠재 결함.

### 7.13 권장 회귀 테스트

- 리더보드 키를 지운 뒤 `rank`·`my-rank`를 호출해 **MySQL 폴백으로 순위가 나오는지**(빈 랭킹이 아닌지) 확인한다.
- 재화 행을 일부러 지운 계정으로 강화·가챠를 호출해 `InsufficientCurrency`가 아니라 서버 결함으로 드러나는지 확인한다.
- 이벤트 로그 sink를 실패시킨 상태에서 스테이지 클리어를 호출해 **200이 나가고 보상이 반영되는지** 확인한다.
- 같은 계정으로 로그인을 동시에 두 번 보내 500이 나지 않는지, Redis를 내린 상태에서 로그아웃했을 때 세션이
  실제로 무효화되는지 확인한다.
- 하트비트와 오프라인 정산을 동시에 보내 정산이 `AlreadyClaimed`로 사라지지 않는지 확인한다.
- 같은 소모품을 동시에 두 번 사용해 `ItemNotFound`가 아니라 두 요청이 차례로 처리되는지 확인한다.
- 256자 이메일로 회원가입해 400이 나오는지 확인한다.

---

## 8. 추가 기능 개발 계획

지금 당장 구현하지는 않지만, 이번 검토에서 필요성이 드러나 **나중에 붙일 만한 기능**을 계획만 적어 둡니다.
여기 있는 항목은 아직 착수하지 않았고, 구현 시점에 세부 기획서를 따로 작성합니다.

| 항목 | 한 줄 요약 | 상태 |
|---|---|---|
| [8.1](#81-크리티컬-이슈-알림-기능) | 크리티컬 이슈 알림 기능 | 계획 |

### 8.1 크리티컬 이슈 알림 기능

**왜 필요한가.** 6.2에서 정한 것처럼, 배치가 스스로 풀 수 없는 상황은 **로그로 드러내고 사람이 보게**
하기로 했습니다. 그런데 지금은 그 로그가 콘솔·파일에만 남아서, **사람이 들여다보기 전까지 아무도 모릅니다.**
보스러시 정산이 멈춰 있어도 다음 시즌이 열리지 않는 걸 유저가 먼저 알아차리는 상황이 생길 수 있습니다.
서버가 "사람이 봐야 한다"고 판단한 순간에 **담당자에게 밀어 주는 경로**가 필요합니다.
