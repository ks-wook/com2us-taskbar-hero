{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      // 서비스가 ErrorCode를 반환할 때 남기는 거절 로그(사용자 입력 실수)는 Debug다 — 개발에서만 켠다.
      "AccountServer": "Debug"
    }
  }
}
